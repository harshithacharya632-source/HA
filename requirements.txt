kurigram==2.2.23
tgcrypto 
pymediainfo
requests 
telegraph
bs4 
shortzy 
pytz 
aiohttp
yt-dlp
youtube-dl
ytthumb
jinja2
apscheduler
pyshorteners
colorama
NumPy 
wheel 
ujson 
wget
aiofiles
hachoir
python-decouple 
ffmpeg
youtube_search
# NOT bumped in this pass - left as-is deliberately:
# motor==2.5.1 requires pymongo<4, and umongo==3.0.1's `motor` extra
# requires motor<3.0. Those two constraints mean pymongo and motor can
# only move together (e.g. pymongo 4.8.0 + motor 3.5.1), and only if
# umongo's motor integration isn't actually used elsewhere in this
# codebase - confirm that first, then bump both at once and test.
# The code-level fix (explicit serverSelectionTimeoutMS/retryReads/
# retryWrites in users_chats_db.py) already works against these
# current pins with zero dependency changes, so it's safe to ship now.
pymongo[srv]==3.12.3 
motor==2.5.1 
marshmallow==3.14.1 
umongo==3.0.1 
PyLeaves==1.0.2
youtube-search-python==1.4.6
opencv-python-headless==4.7.0.68 
googletrans==3.1.0a0 
python-dotenv==0.21.1 
gTTS==2.3.1 
speedtest==0.0.1 
humanize==4.6.0 
Pillow==9.4.0 
pytesseract==0.3.10
psutil==5.9.4 
dnspython==2.3.0
ffmpeg-python>=0.2.0
openai==0.25.0
cinemagoer==2023.5.1
SQLAlchemy<2.0
